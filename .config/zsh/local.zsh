# Used for machine specific aliases and configs (personal, work, server, etc.)
# Changes are ignored but this file stays.

alias setup-local-zsh="git update-index --skip-worktree ~/.config/zsh/local.zsh"